import { createTheme } from "@mui/material";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#03a9f4",
      light: "skyblue",
    },
    secondary: {
      main: "#23f73f",
    },
    otherColor: {
      main: "#999",
    },
  },
});
